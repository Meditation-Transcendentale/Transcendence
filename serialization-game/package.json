{
  "name": "serialization-game",
  "version": "1.0.0",
  "private": true,
  "main": "build/js/index.js",
  "types": "build/types/index.d.ts",
  "scripts": {
	"gen": "flatc --ts --ts-flat-files --filename-ext ts -o src/generated -I schema/*.fbs",
	"generate-barrel":"node scripts/generate-barrel.mjs",
    "build": "npm run gen && npm run generate-barrel && tsc"
  },
  "type": "module",
  "dependencies": {
    "flatbuffers": "25.2.10"
  },
  "devDependencies": {
    "typescript": "^4.x"
  }
}

