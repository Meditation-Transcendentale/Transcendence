// nats.subscribe.match.quit.json
// game.${mode}.${gameId}.match.quit
// mode : "ai", "local", "online", "pongbr", "pongio"
{
	"uuid": {
		"type": "string",
		"format": "uuid",
		"description": "Player’s unique identifier that quit game before end"
	},
}
