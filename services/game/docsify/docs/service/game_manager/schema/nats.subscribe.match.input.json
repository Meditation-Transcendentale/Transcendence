// nats.subscribe.match.input.json
// game.${mode}.${gameId}.match.input
// mode : "ia", "local", "online", "pongbr", "pongio"
{
	"paddleId": {
		"type": "number",
		"description": "Numeric identifier of the paddle being updated"
	},
	"move": {
		"type": "number",
		"description": "Whether the paddle is moving "
	},
}
