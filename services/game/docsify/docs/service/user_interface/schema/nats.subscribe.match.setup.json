// nats.subscribe.match.setup.json
// nats.publishe.match.setup.json
// game.${mode}.${gameId}.match.setup
// mode : "ai", "local", "online", "pongbr", "pongio"
{
	"players": {
		"type": "array",
        "items": { 
			"format": "uuid",
			"type": "string"
		}
	},
}
