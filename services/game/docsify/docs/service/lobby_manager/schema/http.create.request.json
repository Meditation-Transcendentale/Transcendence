// http.create.request.json
{
    "mode": {
      "type": "string",
      "enum": ["local", "ai", "online", "pongio", "pongbr", ],
      "description": "Which game mode to play."
    },
    "map": {
      "type": "string",
      "description": "Which map to use"
    },
}
