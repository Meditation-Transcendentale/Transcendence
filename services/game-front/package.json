{
  "name": "transcendence-frontend",
  "scripts": {
    "build": "tsc && esbuild src/main.ts --bundle --minify --sourcemap --outfile=dist/app.js --target=es2020",
    "dev": "vite --host",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "esbuild": "^0.19.11",
    "typescript": "^5.2.2",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "@babylonjs/core": "^8.0.0",
    "@babylonjs/gui": "8.2.0",
    "@babylonjs/inspector": "^8.3.0",
    "buffer": "^6.0.3"
  }
}
